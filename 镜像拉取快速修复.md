# 🚀 镜像拉取快速修复指南

## 问题症状

构建时出现以下错误：

```
ERROR [internal] load metadata for docker.io/library/node:18-alpine
failed to resolve source metadata: not found
```

## 快速解决（3 步）

### 步骤 1：测试网络

**Linux/macOS：**
```bash
chmod +x 测试镜像拉取.sh
./测试镜像拉取.sh
```

**Windows：**
```cmd
测试镜像拉取.bat
```

### 步骤 2：手动拉取镜像

**如果测试失败，手动拉取：**

**Linux/macOS：**
```bash
chmod +x 手动拉取镜像.sh
./手动拉取镜像.sh
```

**Windows：**
```cmd
手动拉取镜像.bat
```

### 步骤 3：重新部署

**Linux/macOS：**
```bash
sudo ./一键部署.sh
```

**Windows：**
```cmd
start.bat
```

## 一行命令解决

**Linux/macOS：**
```bash
chmod +x 手动拉取镜像.sh 一键部署.sh && ./手动拉取镜像.sh && sudo ./一键部署.sh
```

**Windows：**
```cmd
手动拉取镜像.bat && start.bat
```

## 如果还是失败

### 方案 A：配置镜像源

**Linux：**
```bash
sudo ./fix-docker-mirror.sh
sudo systemctl restart docker
```

**Windows：**
1. 打开 Docker Desktop
2. Settings → Docker Engine
3. 添加以下配置：
```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```
4. Apply & Restart

### 方案 B：手动拉取并标记

```bash
# 从镜像源拉取
docker pull docker.mirrors.ustc.edu.cn/library/node:18-alpine
docker pull docker.mirrors.ustc.edu.cn/library/golang:1.23-alpine
docker pull docker.mirrors.ustc.edu.cn/library/alpine:3.19

# 重新标记
docker tag docker.mirrors.ustc.edu.cn/library/node:18-alpine node:18-alpine
docker tag docker.mirrors.ustc.edu.cn/library/golang:1.23-alpine golang:1.23-alpine
docker tag docker.mirrors.ustc.edu.cn/library/alpine:3.19 alpine:3.19

# 构建
docker compose build
docker compose up -d
```

## 验证成功

```bash
# 检查镜像
docker images | grep -E "node|golang|alpine"

# 应该看到
node        18-alpine    ...
golang      1.23-alpine  ...
alpine      3.19         ...
```

## 需要帮助？

查看详细文档：
- [镜像拉取失败解决方案.md](镜像拉取失败解决方案.md)
- [快速开始.md](快速开始.md)

提交 Issue：
- https://github.com/QwQBiG/qwq-aiops/issues

---

**记住**：先测试，再拉取，最后部署！
