# 🚀 qwq AIOps 平台 - 快速开始

## ⚠️ 重要提示

**qwq 是 AI 驱动的智能运维平台，必须配置 AI 服务才能正常运行！**

部署时会提示选择 AI 服务类型：
- **OpenAI API**：需要 API Key，功能最强（推荐新手）
- **Ollama 本地模型**：完全免费，数据不出服务器（推荐企业）

## 一键部署（推荐）⭐

只需要一个命令：

```bash
chmod +x 一键部署.sh
sudo ./一键部署.sh
```

**就这么简单！** 脚本会自动完成所有配置和部署，包括 AI 服务配置。

## 预期时间

- **首次部署**：6-10 分钟
- **后续部署**：1-2 分钟

## 部署成功后

访问以下地址：

| 服务 | 地址 | 说明 |
|------|------|------|
| **前端界面** | http://localhost:8081 | 主界面 |
| **API 文档** | http://localhost:8081/api/docs | Swagger UI |
| **Prometheus** | http://localhost:9091 | 监控指标 |
| **Grafana** | http://localhost:3000 | 可视化面板 |

**默认账号**：
- 用户名：`admin`
- 密码：`admin123`

## 脚本做了什么

`一键部署.sh` 会自动：

1. ✅ **配置 AI 服务**（OpenAI 或 Ollama）
2. ✅ 配置 Docker 国内镜像源（加速下载）
3. ✅ 创建所需的配置文件
4. ✅ 构建 Docker 镜像
5. ✅ 启动所有服务
6. ✅ 验证服务状态

## AI 服务配置

### 如果跳过了 AI 配置

运行配置向导：

```bash
chmod +x 配置AI服务.sh
./配置AI服务.sh
```

### 如果需要切换 AI 服务

同样运行配置向导，选择新的 AI 服务类型即可。

### 手动配置

编辑 `docker-compose.yml`，找到 AI 配置部分：

```yaml
# 方式 1: OpenAI API
# - AI_PROVIDER=openai
# - OPENAI_API_KEY=sk-your-api-key-here
# - OPENAI_BASE_URL=https://api.openai.com/v1
# - OPENAI_MODEL=gpt-3.5-turbo

# 方式 2: Ollama 本地模型
# - AI_PROVIDER=ollama
# - OLLAMA_HOST=http://host.docker.internal:11434
# - OLLAMA_MODEL=qwen2.5:7b
```

取消某一种方式的注释（删除行首的 `#`），填入正确的配置值，然后重启服务：

```bash
docker compose restart qwq
```

## 常用命令

```bash
# 查看服务状态
docker compose ps

# 查看日志
docker compose logs -f qwq

# 停止服务
docker compose down

# 重启服务
docker compose restart

# 重新部署
sudo ./一键部署.sh
```

## 如果遇到问题

### 问题 1：镜像拉取失败 ⚠️

**错误信息**：
```
ERROR [internal] load metadata for docker.io/library/node:18-alpine
failed to resolve source metadata: not found
```

**解决方案**：

**方案 A：使用手动拉取脚本（推荐）**
```bash
# Linux/macOS
chmod +x 手动拉取镜像.sh
./手动拉取镜像.sh

# Windows
手动拉取镜像.bat
```

**方案 B：配置镜像源后重试**
```bash
# 配置镜像源
sudo ./fix-docker-mirror.sh

# 重新部署
sudo ./一键部署.sh
```

**方案 C：手动拉取并标记**
```bash
# 从国内镜像源拉取
docker pull docker.mirrors.ustc.edu.cn/library/node:18-alpine
docker tag docker.mirrors.ustc.edu.cn/library/node:18-alpine node:18-alpine

docker pull docker.mirrors.ustc.edu.cn/library/golang:1.23-alpine
docker tag docker.mirrors.ustc.edu.cn/library/golang:1.23-alpine golang:1.23-alpine

docker pull docker.mirrors.ustc.edu.cn/library/alpine:3.19
docker tag docker.mirrors.ustc.edu.cn/library/alpine:3.19 alpine:3.19

# 重新构建
docker compose build
docker compose up -d
```

详细解决方案请查看：[镜像拉取失败解决方案.md](镜像拉取失败解决方案.md)

### 问题 2：网络超时

```bash
# 检查网络
ping docker.io

# 测试镜像源
curl -I https://docker.mirrors.ustc.edu.cn/v2/

# 重新运行
sudo ./一键部署.sh
```

### 问题 3：端口被占用

编辑 `docker-compose.yml`，修改端口：

```yaml
ports:
  - "8082:8080"  # 改为其他端口
```

### 问题 4：磁盘空间不足

```bash
# 检查空间
df -h

# 清理 Docker
docker system prune -a
```

## 详细文档

- **[一键部署说明.md](一键部署说明.md)** - 详细的部署说明
- **[START_HERE.md](START_HERE.md)** - 完整的快速开始指南
- **[docs/deployment-guide.md](docs/deployment-guide.md)** - 完整部署指南

## 项目特性

- 🤖 **AI 驱动** - 自然语言运维交互
- 🔒 **数据安全** - 支持本地私有模型
- 🚀 **一站式管理** - 应用商店、容器、网站、数据库
- ⚡ **高可用架构** - 集群部署、负载均衡
- ✅ **生产就绪** - 13 个核心属性，96+ 子属性测试

## 系统要求

- **Docker**: 20.10+
- **Docker Compose**: 2.0+
- **内存**: 4GB+（推荐 8GB+）
- **磁盘**: 20GB+（推荐 50GB+）
- **CPU**: 2 核+（推荐 4 核+）

## GitHub 仓库

https://github.com/QwQBiG/qwq-aiops

## 许可证

MIT License. Copyright (c) 2025 qwqBig.

---

**开始使用吧！** 🎉

```bash
sudo ./一键部署.sh
```
